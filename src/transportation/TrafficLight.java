package transportation;

public class TrafficLight {
	//Internal Timer that sets light to red, yellow, green. 
	//Will have public data that will allow the bus to see what color the traffic light currently is. 
	//Bus will check state of the light at specific coordinates when it is approaching a stoplight.
	//if the stoplight color is red, then the bus will set its speed to zero.  
	//if the stoplight color is yellow, bus will either speed up or slow down (depends on which is easier to implement)
}
